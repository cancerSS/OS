import 'package:flutter/material.dart';

class Stylet extends StatelessWidget {
  Stylet({super.key});
  Widget build(context) {
    return Text("Hello IS",
      style: TextStyle(fontSize: 28, 
      color: Colors.fromARGB(255, 247, 242, 245)));
  }
}
